# node-heroku-template

* change `"name"` in `package.json`
* run:
```
yarn add express
yarn add -D typescript ts-node-dev @types/node @types/express

yarn dev
```
* optional: remove README.md and LICENSE
